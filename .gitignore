# no emacs temp files
*~
# no tables files
*.tex
*.aux
*.log
*.pdf
# no python compiled files:
*pyc
# no python init file
python/__init__.py
# no lip batch files
scripts/lip-batch/*.sh.e*
scripts/lip-batch/*.sh.o*
# no plots directory, nor files inside it
plots
plots/
# no datacards and shapes directory, no files inside them
datacards
datacards/
shapes
shapes/
